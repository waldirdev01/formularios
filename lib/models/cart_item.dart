class CartItem {
  final String id, productId, name;
  final int quantinty;
  final double price;

  CartItem(
      {required this.id,
      required this.productId,
      required this.name,
      required this.quantinty,
      required this.price});
}
